# Frontend environment variables
# Copy to .env.local and fill in for local development

# Backend API URL
# Leave empty to use Vite's proxy (recommended for local dev)
# Set to your deployed backend URL for production builds
VITE_API_URL=
